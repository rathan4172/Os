#include<stdio.h>
#include<unistd.h>

int main()
{
    printf("Process ID: %d\n", getpid());  // Prints the current process ID
    printf("Parent Process ID: %d\n", getppid());  // Prints the parent process ID
    return 0;
}
